<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Bramble AutoFill</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Required to read the biometric-gated VEK Keychain item (Face ID) from the
	     extension; without it the biometric read fails and nothing can be decrypted. -->
	<key>NSFaceIDUsageDescription</key>
	<string>Bramble uses Face ID to unlock and fill your saved passwords.</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.authentication-services-credential-provider-ui</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).CredentialProviderViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- iOS 18+ requires declaring what the provider supplies, or it isn't
			     listed as an AutoFill source. -->
			<key>ASCredentialProviderExtensionCapabilities</key>
			<dict>
				<key>ProvidesPasswords</key>
				<true/>
				<key>ProvidesOneTimeCodes</key>
				<true/>
				<!-- Passkey provider (iOS 17+). The provider methods are availability-gated
				     in code so the extension still runs on iOS 15/16 for passwords. -->
				<key>ProvidesPasskeys</key>
				<true/>
				<!-- Credential exchange (iOS 26+). Declared on the EXTENSION even though the
				     transfer runs in the app: this is what makes the OS list Bramble in the
				     import/export picker. Older systems ignore both keys.
				     See docs/credential-exchange.md. -->
				<key>SupportsCredentialExchange</key>
				<true/>
				<key>SupportedCredentialExchangeVersions</key>
				<array>
					<string>1.0</string>
				</array>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
